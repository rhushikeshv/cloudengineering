name: Pulumi
on:
  push:
    branches:
      - master
jobs:
  up:
    name: Preview
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/setup-node@v1 #this will install Node and npm on Ubuntu
        with:
          node-version: '12.x'
      - name: Checkout code
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm install -g @angular/cli
      - run: pwd
      - run: ls /home/runner/work/pulumi
      - run: ls /home/runner/work
      - name: Build
        run: ng build
        working-directory: ./aws-python/frontend/pulumi-app
       
      
      
      

